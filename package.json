{
  "name": "astgen",
  "version": "0.1.0",
  "description": "Generation of AST types and helper functions",
  "main": "lib/index.js",
  "scripts": {
    "sample": "yarn sample:expr && yarn sample:repmin && yarn sample:kitchen",
    "sample:expr": "ts-node scripts/astgen.ts samples/expr.yaml | tee samples/expr.ts",
    "sample:repmin": "ts-node scripts/astgen.ts samples/repmin.yaml | tee samples/repmin.ts",
    "sample:kitchen": "ts-node scripts/astgen.ts samples/kitchen.yaml | tee samples/kitchen.ts",
    "sample:dogfood": "ts-node scripts/astgen.ts samples/dogfood.yaml | tee samples/dogfood.ts",
    "build": "tsc",
    "test": "jest"
  },
  "keywords": [
    "AST"
  ],
  "author": "Michael M. Tiller",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^9.0.12",
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "clipanion": "^3.0.0-rc.12",
    "fs-extra": "^10.0.0",
    "purify-ts": "^0.16.3",
    "yaml": "^1.10.2"
  }
}
