nodes:
  FieldType:
    BuiltinType:
      - types: <string>
    EnumType:
      - tags: string[]
    NodeType:
      - types: string[]
  ASTTree:
    ASTUnionType:
      - name: string
      - subtypes: string[]
    ASTLeafType:
      - tag: string
      - rootUnion: ASTUnionType
      - name: string
      - bases: string[]
      - fields: Field{}
externs:
  - Field

options:
  tagName: kind
  maps: map
